#!/usr/bin/env node
'use strict';

var program = require('../src/index');
var meow = require('meow');
var cli = meow(`
    Usage
      $ sentiment <query>...

    Examples
      $ sentiment trump
`);
var queries = cli.input;
if (!queries.length) {
    cli.showHelp(1);
}
program(queries);
